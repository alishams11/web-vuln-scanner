id: xss-reflected
name: Reflected XSS
severity: high

requests:
  - method: GET
    path: "/?q={{payload}}"
    payloads:
      payload:
        - "<script>alert(1)</script>"

matchers:
  - type: word
    part: body
    words:
      - "<script>alert(1)</script>"

